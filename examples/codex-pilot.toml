# Copy only when you want persistent Codex Pilot overrides.
policy = "quality"

allow_fast = false
allow_ultra = true

min_reasoning = "medium"
max_reasoning = "max"

max_escalations = 2
max_agents = 3

ponytail = "required"

show_routing = true

suggest_parent_upgrade = true
suggest_parent_downgrade = true
